# SAFETY

このドキュメントは「懸賞キャンペーン応募支援ダッシュボード」の安全設計と禁止機能をまとめたものです。

## このアプリの目的

このアプリの目的は、X、Instagram、TikTok、公式サイトなどで見つけたプレゼントキャンペーンや懸賞キャンペーンを整理し、人間が手動で応募しやすくすることです。

応募条件、締切、リスク、コメント案、応募履歴、応募後フォローを見やすく管理するためのローカルWebアプリであり、SNS操作や応募操作を代行するものではありません。

## 実装していない機能

以下の機能は実装していません。今後も原則として実装しません。

- Xへの自動ログイン
- Instagramへの自動ログイン
- TikTokへの自動ログイン
- 自動フォロー
- 自動リポスト
- 自動いいね
- 自動コメント
- 自動DM
- 自動投稿
- SNS APIによる自動応募
- 複数アカウント操作
- CAPTCHA回避
- レート制限回避
- アカウント凍結回避
- Playwright / Puppeteer などによるSNS応募操作
- 外部フォームの自動送信

## なぜSNS自動操作を実装しないのか

SNSの自動操作や大量応募は、各サービスの規約違反、スパム判定、アカウント制限、アカウント凍結、個人情報漏えい、意図しない投稿やDM送信につながるリスクがあります。

このアプリは、規約に沿ってユーザー自身が判断し、手動で応募するための管理・記録支援に限定します。

## 自動化してよい範囲

このアプリで自動化してよい範囲は、ユーザーの手元のデータ整理と判断補助に限ります。

- キャンペーン情報の整理
- 本文のキーワード解析補助
- リスク判定
- 応募優先度スコア計算
- コメント案生成
- 手動応募チェックリスト
- 応募記録
- 応募後フォローのリマインド表示
- 締切日・確認予定日の画面内リマインダー
- ユーザーが明示的に許可した場合のみのブラウザ通知
- ICSカレンダー出力
- JSON/CSVバックアップ
- ローカルデータの検索、フィルター、タグ管理

コメント案は下書き補助です。同じ文面の大量投稿や自動コメントを前提にしません。

## 自動化しない範囲

以下は必ず人間が手動で行います。

- フォロー
- リポスト
- いいね
- コメント
- DM確認
- ログイン
- 投稿
- 外部フォーム送信
- 個人情報入力
- クレジットカード情報入力
- LINE登録
- キャンペーン応募完了操作

アプリ内の「投稿を開く」はURLを新しいタブで開くだけです。その先の操作はユーザーが自分で確認して行います。

## リマインダーと通知

リマインダーは、締切日、確認予定日、連絡待ち、発送待ち、受取待ちを見落としにくくするための表示機能です。

ブラウザ通知は任意です。ユーザーが「通知を許可」ボタンを押した場合のみ、ブラウザの通知許可確認を行います。通知が拒否された場合もアプリは通常通り使えます。

通知やICS出力はSNS操作、応募操作、外部フォーム送信を行いません。

## URL安全性チェック

投稿URLは以下の方針で扱います。

- `http://` または `https://` のURLのみ許可します。
- `javascript:` URLは拒否します。
- `ftp:`、`data:` などは拒否します。
- 不正なURLは保存時に空欄へ補正します。

ただし、URLが形式上安全でもキャンペーン自体が安全とは限りません。外部サイト、短縮URL、LINE登録、個人情報入力、手数料要求などはリスク判定で注意喚起します。

## インポート検証

JSONインポートでは以下を検証します。

- JSON全体が配列であること
- 各要素がキャンペーンオブジェクトであること
- URLが安全な形式であること
- 日付が `YYYY-MM-DD` であること
- タグが配列または文字列であること
- 応募条件、ステータス、SNS種別が許可値であること
- 件数が上限を超えないこと

不正なJSONは読み込まず、画面上のエラー表示に留めます。

## localStorage破損時の復旧

localStorageに壊れたJSONが保存されていた場合、アプリは保存データを削除し、空配列として復旧します。

復旧により画面が壊れることは避けられますが、壊れたデータは復元できません。重要なデータは定期的にJSONエクスポートしてください。

## データ保存上の注意

- データはブラウザの localStorage に保存されます。
- localStorageキーは `kensho_campaign_dashboard_v1` です。
- 別ブラウザ、別端末、別プロファイルとは自動共有されません。
- ブラウザの閲覧データ削除で消える可能性があります。
- バックアップにはJSONエクスポートを使ってください。
- CSVは分析・閲覧向けで、完全な復元用途にはJSONを推奨します。
- ICSはカレンダー登録向けで、完全な復元用途にはJSONを推奨します。

## 今後の拡張時の安全方針

リマインダー、Googleスプレッドシート連携、GitHub Pages公開などを追加する場合も、SNS操作や応募操作の自動化は行いません。

外部連携を追加する場合は、ユーザーが明示的にエクスポート・同期を実行する設計を優先し、個人情報や応募履歴の扱いをREADMEと画面上で明示します。
